---
ms.date: 2017-06-05
keywords: PowerShell cmdlet'i
title: "Windows PowerShell adlarını öğrenme"
ms.assetid: b4d0fd22-8298-4ee6-82ae-9b6f2907c986
ms.openlocfilehash: 28c821c4a617b6ac775dbdda8ade3d15c3f218c3
ms.sourcegitcommit: 74255f0b5f386a072458af058a15240140acb294
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 08/03/2017
---
# <a name="learning-windows-powershell-names"></a><span data-ttu-id="ef70e-103">Windows PowerShell adlarını öğrenme</span><span class="sxs-lookup"><span data-stu-id="ef70e-103">Learning Windows PowerShell Names</span></span>
<span data-ttu-id="ef70e-104">Önemli zaman yatırım çok komut satırı arabirimi ile komutları ve komut parametreleri öğrenme adları var.</span><span class="sxs-lookup"><span data-stu-id="ef70e-104">Learning names of commands and command parameters is a significant time investment with most command-line interfaces.</span></span> <span data-ttu-id="ef70e-105">Her komut ve düzenli olarak kullanmak için gereken her bir parametreyi öğrenerek öğrenmek kaldırmanın tek yolu olması için çok az desenleri olduğunu konudur.</span><span class="sxs-lookup"><span data-stu-id="ef70e-105">The issue is that there are very few patterns, so the only way to learn is by memorizing each command and each parameter that you need to use on a regular basis.</span></span>

<span data-ttu-id="ef70e-106">Yeni bir komut veya parametre ile çalışırken, önceden bilmeniz genellikle kullanamazsınız; ve yeni bir ad öğrenin gerekir.</span><span class="sxs-lookup"><span data-stu-id="ef70e-106">When you work with a new command or parameter, you cannot generally use what you already know; you have to find and learn a new name.</span></span> <span data-ttu-id="ef70e-107">Nasıl arabirimleri artımlı eklemelerle araçları küçük bir dizi işlevsellik büyümesine bakarsanız, neden yapısı standart olmayan olduğunu görmek kolaydır.</span><span class="sxs-lookup"><span data-stu-id="ef70e-107">If you look at how interfaces grow from a small set of tools with incremental additions to functionality, it is easy to see why the structure is nonstandard.</span></span> <span data-ttu-id="ef70e-108">Komut adlarıyla özellikle, bu yana her komutu ayrı bir araçtır, ancak komut adlarının işlemek için daha iyi bir yolu yoktur mantıksal ses.</span><span class="sxs-lookup"><span data-stu-id="ef70e-108">With command names in particular, this may sound logical since each command is a separate tool, but there is a better way to handle command names.</span></span>

<span data-ttu-id="ef70e-109">Komutların çoğu işletim sistemi veya hizmet veya işlemler gibi uygulamaları öğelerini yönetmek için tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="ef70e-109">Most commands are built to manage elements of the operating system or applications, such as services or processes.</span></span> <span data-ttu-id="ef70e-110">Komutların olabilir veya bir ailesi uygun değildir adlarını çeşitli vardır.</span><span class="sxs-lookup"><span data-stu-id="ef70e-110">The commands have a variety of names that may or may not fit into a family.</span></span> <span data-ttu-id="ef70e-111">Örneğin, Windows sistemlerinde kullanabileceğiniz **net start** ve **net stop** bir hizmeti durdurmak ve başlatmak için komutları.</span><span class="sxs-lookup"><span data-stu-id="ef70e-111">For example, on Windows systems, you can use the **net start** and **net stop** commands to start and stop a service.</span></span> <span data-ttu-id="ef70e-112">Windows için tamamen farklı bir ada sahip başka bir daha genelleştirilmiş hizmet denetim aracı yoktur **sc**, değil sığması için adlandırma desenine **net** hizmet komutları.</span><span class="sxs-lookup"><span data-stu-id="ef70e-112">There is another more generalized service control tool for Windows that has a completely different name, **sc**, that does not fit into the naming pattern for the **net** service commands.</span></span> <span data-ttu-id="ef70e-113">İşlem yönetimi için Windows sahip **tasklist** listesi işlemlere komutunu ve **taskkill** işlemleri KILL komutu.</span><span class="sxs-lookup"><span data-stu-id="ef70e-113">For process management, Windows has the **tasklist** command to list processes and the **taskkill** command to kill processes.</span></span>

<span data-ttu-id="ef70e-114">Parametre almaz komutları düzensiz parametre belirtimlerine sahip.</span><span class="sxs-lookup"><span data-stu-id="ef70e-114">Commands that take parameters have irregular parameter specifications.</span></span> <span data-ttu-id="ef70e-115">Kullanamazsınız **net start** bir uzak bilgisayarda bir hizmeti başlatmak için komutu.</span><span class="sxs-lookup"><span data-stu-id="ef70e-115">You cannot use the **net start** command to start a service on a remote computer.</span></span> <span data-ttu-id="ef70e-116">**Sc** komutu, bir uzak bilgisayarda bir hizmet başlayacak, ancak uzak bilgisayarı belirtmek için çift ters eğik çizgi adıyla önek.</span><span class="sxs-lookup"><span data-stu-id="ef70e-116">The **sc** command will start a service on a remote computer, but to specify the remote computer, you must prefix its name with a double backslash.</span></span> <span data-ttu-id="ef70e-117">Örneğin, DC01 adlı bir uzak bilgisayarda Biriktirici hizmetini başlatmak için şunu yazın **sc \\ \\DC01 Başlat biriktirici**.</span><span class="sxs-lookup"><span data-stu-id="ef70e-117">For example, to start the spooler service on a remote computer named DC01, you would type **sc \\\\DC01 start spooler**.</span></span> <span data-ttu-id="ef70e-118">Listeye kullanmanıza gerek DC01 üzerinde çalışan görevler **/S** parametresi ("Sistem" için) ve kaynağı şöyle ters eğik çizgi olmadan DC01 adı: **tasklist /S DC01**.</span><span class="sxs-lookup"><span data-stu-id="ef70e-118">To list tasks running on DC01, you need to use the **/S** (for "system") parameter and supply the name DC01 without backslashes, like this: **tasklist /S DC01**.</span></span>

<span data-ttu-id="ef70e-119">Bir hizmet ve işlem arasındaki önemli teknik farklılıklar olsa da, bunlar öğelerinin iyi tanımlanmış bir yaşam döngüsü sahip yönetilebilir bir bilgisayarda her iki örnek verilebilir.</span><span class="sxs-lookup"><span data-stu-id="ef70e-119">Although there are important technical distinctions between a service and a process, they are both examples of manageable elements on a computer that have a well-defined life cycle.</span></span> <span data-ttu-id="ef70e-120">Tüm hizmetler ve işlemlerin şu anda çalışan bir listesini alma başlatmak veya bir hizmet veya işlemi durdurmak isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="ef70e-120">You may want to start or stop a service or process, or get a list of all currently running services or processes.</span></span> <span data-ttu-id="ef70e-121">Bir hizmet ve işlem farklı işlemler olsa da, diğer bir deyişle, sizi bir hizmet veya bir işlem eylemlere genellikle kavramsal olarak aynıdır.</span><span class="sxs-lookup"><span data-stu-id="ef70e-121">In other words, although a service and a process are different things, the actions we perform on a service or a process are often conceptually the same.</span></span> <span data-ttu-id="ef70e-122">Ayrıca, bir eylem parametrelerini belirterek özelleştirmek için yapabilir seçenekleri de kavramsal olarak benzer olabilir.</span><span class="sxs-lookup"><span data-stu-id="ef70e-122">Furthermore, choices we may make to customize an action by specifying parameters may be conceptually similar as well.</span></span>

<span data-ttu-id="ef70e-123">Windows PowerShell anlamak ve cmdlet'lerini kullanmak için bilmeniz gereken farklı ad sayısını azaltmak için bu benzerlikler yararlanan.</span><span class="sxs-lookup"><span data-stu-id="ef70e-123">Windows PowerShell exploits these similarities to reduce the number of distinct names you need to know to understand and use cmdlets.</span></span>

### <a name="cmdlets-use-verb-noun-names-to-reduce-command-memorization"></a><span data-ttu-id="ef70e-124">Komut anımsama zahmetinden azaltmak için fiil-isim adları cmdlet'leri kullan</span><span class="sxs-lookup"><span data-stu-id="ef70e-124">Cmdlets Use Verb-Noun Names to Reduce Command Memorization</span></span>
<span data-ttu-id="ef70e-125">Windows PowerShell, burada her cmdlet adı ile belirli bir isim kısa çizgiyle ayrılmış olarak standart bir fiil oluşan bir "fiil-isim" adlandırma sistemi kullanır.</span><span class="sxs-lookup"><span data-stu-id="ef70e-125">Windows PowerShell uses a "verb-noun" naming system, where each cmdlet name consists of a standard verb hyphenated with a specific noun.</span></span> <span data-ttu-id="ef70e-126">Windows PowerShell fiillerini her zaman İngilizce fiiller değildir, ancak Windows PowerShell'de belirli eylemleri express.</span><span class="sxs-lookup"><span data-stu-id="ef70e-126">Windows PowerShell verbs are not always English verbs, but they express specific actions in Windows PowerShell.</span></span> <span data-ttu-id="ef70e-127">İsimleri çok herhangi bir dilde isimleri gibi, belirli Sistem Yönetimi'nde önemli olan nesne türlerini tanımlar.</span><span class="sxs-lookup"><span data-stu-id="ef70e-127">Nouns are very much like nouns in any language, they describe specific types of objects that are important in system administration.</span></span> <span data-ttu-id="ef70e-128">Fiiller ve isimleri birkaç örnekleri bakarak bu iki parçalı adları öğrenme çaba nasıl azaltmak göstermek kolaydır.</span><span class="sxs-lookup"><span data-stu-id="ef70e-128">It is easy to demonstrate how these two-part names reduce learning effort by looking at a few examples of verbs and nouns.</span></span>

<span data-ttu-id="ef70e-129">İsimleri daha az kısıtlanmış, ancak bunlar bir komut temel aldığı her zaman açıklamalıdır.</span><span class="sxs-lookup"><span data-stu-id="ef70e-129">Nouns are less restricted, but they should always describe what a command acts upon.</span></span> <span data-ttu-id="ef70e-130">Windows PowerShell komutları gibi sahip **Get-Process**, **Stop-Process**, **Get-Service**, ve **Hizmeti Durdur**.</span><span class="sxs-lookup"><span data-stu-id="ef70e-130">Windows PowerShell has commands such as **Get-Process**, **Stop-Process**, **Get-Service**, and **Stop-Service**.</span></span>

<span data-ttu-id="ef70e-131">İki isimleri ve iki fiilleri olması durumunda tutarlılık o kadarlık öğrenme basitleştirmek değil.</span><span class="sxs-lookup"><span data-stu-id="ef70e-131">In the case of two nouns and two verbs, consistency does not simplify learning that much.</span></span> <span data-ttu-id="ef70e-132">Ancak, standart dizi 10 fiilleri ve 10 isimleri bakarsanız, ardından anlamak için sadece 20 sözcükler sahip, ancak bu sözcükleri 100 ayrı komut adlarını oluşturmak için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="ef70e-132">However, if you look at a standard set of 10 verbs and 10 nouns, you then have only 20 words to understand, but those words can be used to form 100 distinct command names.</span></span>

<span data-ttu-id="ef70e-133">Sık sık adını okuyarak komut yaptığı tanıyabilir ve genellikle görünen adı için yeni bir komut kullanılmalıdır.</span><span class="sxs-lookup"><span data-stu-id="ef70e-133">Frequently, you can recognize what a command does by reading its name, and it is usually apparent what name should be used for a new command.</span></span> <span data-ttu-id="ef70e-134">Örneğin, bir bilgisayar kapatma komutunu olabilir **Stop-Computer**.</span><span class="sxs-lookup"><span data-stu-id="ef70e-134">For example, a computer shutdown command might be **Stop-Computer**.</span></span> <span data-ttu-id="ef70e-135">Ağdaki tüm bilgisayarları listeleyen bir komut olabilir **Get-bilgisayar**.</span><span class="sxs-lookup"><span data-stu-id="ef70e-135">A command that lists all computers on a network might be **Get-Computer**.</span></span> <span data-ttu-id="ef70e-136">Sistem tarihi alır komut **Get-Date**.</span><span class="sxs-lookup"><span data-stu-id="ef70e-136">The command that gets the system date is **Get-Date**.</span></span>

<span data-ttu-id="ef70e-137">Belirli bir fiil ile dahil tüm komutları listeleyebilirsiniz **-fiil** parametresi için **Get-Command** (aşağıdakiler ele alınacaktır **Get-Command** sonraki bölümünde ayrıntılı olarak).</span><span class="sxs-lookup"><span data-stu-id="ef70e-137">You can list all commands that include a particular verb with the **-Verb** parameter for **Get-Command** (We will discuss **Get-Command** in detail in the next section).</span></span> <span data-ttu-id="ef70e-138">Örneğin, fiili kullanan tüm cmdlet'leri görmek için **almak**, türü:</span><span class="sxs-lookup"><span data-stu-id="ef70e-138">For example, to see all cmdlets that use the verb **Get**, type:</span></span>

```
PS> Get-Command -Verb Get
CommandType     Name                            Definition
-----------     ----                            ----------
Cmdlet          Get-Acl                         Get-Acl [[-Path] <String[]>]...
Cmdlet          Get-Alias                       Get-Alias [[-Name] <String[]...
Cmdlet          Get-AuthenticodeSignature       Get-AuthenticodeSignature [-...
Cmdlet          Get-ChildItem                   Get-ChildItem [[-Path] <Stri...
...
```

<span data-ttu-id="ef70e-139">**-İsim** parametre olduğundan daha kullanışlı, aynı nesne türünü etkileyen komutları ailesi görmenize olanak sağlar.</span><span class="sxs-lookup"><span data-stu-id="ef70e-139">The **-Noun** parameter is even more useful because it allows you to see a family of commands that affect the same type of object.</span></span> <span data-ttu-id="ef70e-140">Örneğin, görmek istiyorsanız hangi komutların türü komutu aşağıdaki hizmetleri yönetmek için kullanılabilir:</span><span class="sxs-lookup"><span data-stu-id="ef70e-140">For example, if you want to see which commands are available for managing services, type following command:</span></span>

```
PS> Get-Command -Noun Service
CommandType     Name                            Definition
-----------     ----                            ----------
Cmdlet          Get-Service                     Get-Service [[-Name] <String...
Cmdlet          New-Service                     New-Service [-Name] <String>...
Cmdlet          Restart-Service                 Restart-Service [-Name] <Str...
Cmdlet          Resume-Service                  Resume-Service [-Name] <Stri...
Cmdlet          Set-Service                     Set-Service [-Name] <String>...
Cmdlet          Start-Service                   Start-Service [-Name] <Strin...
Cmdlet          Stop-Service                    Stop-Service [-Name] <String...
Cmdlet          Suspend-Service                 Suspend-Service [-Name] <Str... 
...
```

<span data-ttu-id="ef70e-141">Yalnızca bir fiil-isim adlandırma şeması olduğundan komut bir cmdlet değil.</span><span class="sxs-lookup"><span data-stu-id="ef70e-141">A command is not necessarily a cmdlet, just because it has a verb-noun naming scheme.</span></span> <span data-ttu-id="ef70e-142">Bir cmdlet değil ancak bir fiil-isim ada sahip yerel bir Windows PowerShell komutu örneğidir Clear-Host konsol penceresi temizlemek için komutu.</span><span class="sxs-lookup"><span data-stu-id="ef70e-142">One example of a native Windows PowerShell command that is not a cmdlet but has a verb-noun name, is the command for clearing a console window, Clear-Host.</span></span> <span data-ttu-id="ef70e-143">Get-Command karşı çalıştırırsanız gördüğünüz Clear-Host komut gerçekte bir iç işlevi şu şekildedir:</span><span class="sxs-lookup"><span data-stu-id="ef70e-143">The Clear-Host command is actually an internal function, as you can see if you run Get-Command against it:</span></span>

```
PS> Get-Command -Name Clear-Host

CommandType     Name                            Definition
-----------     ----                            ----------
Function        Clear-Host                      $spaceType = [System.Managem...
```

### <a name="cmdlets-use-standard-parameters"></a><span data-ttu-id="ef70e-144">Cmdlet'leri kullanma standart parametreler</span><span class="sxs-lookup"><span data-stu-id="ef70e-144">Cmdlets Use Standard Parameters</span></span>
<span data-ttu-id="ef70e-145">Daha önce belirtildiği gibi geleneksel komut satırı arabirimi kullanılan komutlar tutarlı parametre adları genellikle gerekmez.</span><span class="sxs-lookup"><span data-stu-id="ef70e-145">As noted earlier, commands used in traditional command-line interfaces do not generally have consistent parameter names.</span></span> <span data-ttu-id="ef70e-146">Bazen parametre adları hiç gerekmez.</span><span class="sxs-lookup"><span data-stu-id="ef70e-146">Sometimes parameters do not have names at all.</span></span> <span data-ttu-id="ef70e-147">Bunu yaptıklarında, bunlar genellikle tek karakter veya hızlı bir şekilde yazılabilir, ancak yeni kullanıcılar tarafından kolayca anlaşılmayan sözcükler kısaltılır.</span><span class="sxs-lookup"><span data-stu-id="ef70e-147">When they do, they are often single-character or abbreviated words that can be typed rapidly but are not easily understood by new users.</span></span>

<span data-ttu-id="ef70e-148">Çoğu diğer geleneksel komut satırı arabirimlerinden aksine Windows PowerShell parametreleri doğrudan işler ve parametre adları standartlaştırmak için bu doğrudan erişim Geliştirici Kılavuzu birlikte parametreleri için kullanır.</span><span class="sxs-lookup"><span data-stu-id="ef70e-148">Unlike most other traditional command-line interfaces, Windows PowerShell processes parameters directly, and it uses this direct access to the parameters along with developer guidance to standardize parameter names.</span></span> <span data-ttu-id="ef70e-149">Bu her cmdlet her zaman standartlarına uygun garanti etmez ancak bunun öneririz.</span><span class="sxs-lookup"><span data-stu-id="ef70e-149">Although this does not guarantee that every cmdlet will always conform to the standards, it does encourage it.</span></span>

> [!NOTE]
> <span data-ttu-id="ef70e-150">Parametre adları her zaman sahip bir '-' bunları açıkça bunları parametre olarak tanımlamak Windows PowerShell izin verecek şekilde kullandığınızda bunları etkileşimlidir.</span><span class="sxs-lookup"><span data-stu-id="ef70e-150">Parameter names always have a '-' prepended to them when you use them, to allow Windows PowerShell to clearly identify them as parameters.</span></span> <span data-ttu-id="ef70e-151">İçinde **Get-Command - Name Clear-Host** örnektir, parametrenin adı **adı**, ancak - olarak girilir**adı**.</span><span class="sxs-lookup"><span data-stu-id="ef70e-151">In the **Get-Command -Name Clear-Host** example, the parameter's name is **Name**, but it is entered as -**Name**.</span></span>

<span data-ttu-id="ef70e-152">Standart parametre adları ve kullanımları genel özelliklerinin bazıları aşağıda verilmiştir.</span><span class="sxs-lookup"><span data-stu-id="ef70e-152">Here are some of the general characteristics of the standard parameter names and usages.</span></span>

#### <a name="the-help-parameter-"></a><span data-ttu-id="ef70e-153">Yardım parametresi (?)</span><span class="sxs-lookup"><span data-stu-id="ef70e-153">The Help Parameter (?)</span></span>
<span data-ttu-id="ef70e-154">Belirttiğinizde **-?**</span><span class="sxs-lookup"><span data-stu-id="ef70e-154">When you specify the **-?**</span></span> <span data-ttu-id="ef70e-155">herhangi bir cmdlet'i, cmdlet parametresi olarak yürütülemiyor.</span><span class="sxs-lookup"><span data-stu-id="ef70e-155">parameter to any cmdlet, the cmdlet is not executed.</span></span> <span data-ttu-id="ef70e-156">Bunun yerine, Windows PowerShell cmdlet'i için yardımı görüntüler.</span><span class="sxs-lookup"><span data-stu-id="ef70e-156">Instead, Windows PowerShell displays help for the cmdlet.</span></span>

#### <a name="common-parameters"></a><span data-ttu-id="ef70e-157">Ortak parametreleri</span><span class="sxs-lookup"><span data-stu-id="ef70e-157">Common Parameters</span></span>
<span data-ttu-id="ef70e-158">Windows PowerShell sahip olarak bilinen birkaç parametre *ortak parametreler*.</span><span class="sxs-lookup"><span data-stu-id="ef70e-158">Windows PowerShell has several parameters known as *common parameters*.</span></span> <span data-ttu-id="ef70e-159">Bir cmdlet tarafından uygulanan her Bu parametreler Windows PowerShell altyapısı tarafından kontrol edilir çünkü bunlar her zaman aynı şekilde davranır.</span><span class="sxs-lookup"><span data-stu-id="ef70e-159">Because these parameters are controlled by the Windows PowerShell engine, whenever they are implemented by a cmdlet, they will always behave the same way.</span></span> <span data-ttu-id="ef70e-160">Ortak parametreler **whatIf**, **Onayla**, **ayrıntılı**, **hata ayıklama**, **uyar**, **ErrorAction**, **ErrorVariable**, **OutVariable**, ve **OutBuffer**.</span><span class="sxs-lookup"><span data-stu-id="ef70e-160">The common parameters are **WhatIf**, **Confirm**, **Verbose**, **Debug**, **Warn**, **ErrorAction**, **ErrorVariable**, **OutVariable**, and **OutBuffer**.</span></span>

#### <a name="suggested-parameters"></a><span data-ttu-id="ef70e-161">Önerilen parametreleri</span><span class="sxs-lookup"><span data-stu-id="ef70e-161">Suggested Parameters</span></span>
<span data-ttu-id="ef70e-162">Windows PowerShell çekirdek cmdlet'lerinin benzer parametreleri için standart adlarını kullanın.</span><span class="sxs-lookup"><span data-stu-id="ef70e-162">The Windows PowerShell core cmdlets use standard names for similar parameters.</span></span> <span data-ttu-id="ef70e-163">Parametre adları kullanımını zorlanmaz rağmen Standartlaştırma teşvik eden kullanım için açık bir yönerge yoktur.</span><span class="sxs-lookup"><span data-stu-id="ef70e-163">Although the use of parameter names is not enforced, there is explicit guidance for usage to encourage standardization.</span></span>

<span data-ttu-id="ef70e-164">Örneğin, bir parametre adlar bir bilgisayar adı olarak tarafından Kılavuzu önerir **ComputerName**, sunucu, ana bilgisayar, sistem, düğüm veya ortak alternatif sözcükleri yerine.</span><span class="sxs-lookup"><span data-stu-id="ef70e-164">For example, the guidance recommends naming a parameter that refers to a computer by name as **ComputerName**, rather than Server, Host, System, Node, or other common alternative words.</span></span> <span data-ttu-id="ef70e-165">Önemli önerilen parametre arasında adlardır **zorla**, **hariç**, **INCLUDE**, **PassThru**, **yolu**, ve **CaseSensitive**.</span><span class="sxs-lookup"><span data-stu-id="ef70e-165">Among the important suggested parameter names are **Force**, **Exclude**, **Include**, **PassThru**, **Path**, and **CaseSensitive**.</span></span>

